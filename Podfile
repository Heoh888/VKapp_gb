# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VKapp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "PromiseKit", "~> 6.8"
  # Pods for VKapp

  target 'VKappTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VKappUITests' do
    # Pods for testing
  end

end
